{
  "version": 2,
  "builds": [
    {
      "src": "front-end/build",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dest"
      }
    },
    {
      "src": "back-end/package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["back-end/.env"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/back-end/src/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/front-end/build/$1"
    }
  ]
}
